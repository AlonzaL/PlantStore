# PlantStore - Android App

PlantStore — это современное мобильное приложение для Android, представляющее собой элегантный интернет-магазин растений. Приложение построено с нуля на Kotlin с использованием Jetpack Compose и интегрировано с Firebase для работы с данными и аутентификации в реальном времени.

## 🎨 Дизайн и UI/UX

Визуальная часть приложения и пользовательский опыт основаны на публичном UI-ките **"Kimi - Free Plant Store Mobile UI Kit"** от Pixelbites. Это позволило сосредоточиться на технической реализации и переносе качественного дизайна в код на Jetpack Compose.

| Главный экран | Экран деталей |
|:---:|:---:|
| <img src="https://github.com/user-attachments/assets/506098d9-0560-4e33-939f-18a62067506f" width="300"/> | <img src="https://github.com/user-attachments/assets/7483065c-3bef-4e21-aa73-8abe39f69cb9" width="300"/> |
*   **[Ознакомиться с макетом в Figma](https://www.figma.com/design/ou91bezCgVrAppY7cBVOup/kimi-free-plant-store-mobile-ui-kit?m=auto&t=Zg7psAoy8g5iKcu9-6)**

## 🌿 Ключевые возможности

*   **Современный UI:** Весь интерфейс построен на Jetpack Compose с использованием Material 3, что обеспечивает плавную анимацию и отзывчивый дизайн.
*   **Динамическая главная страница:** Отображает секции "Popular" и "New", загружая данные напрямую из Firestore. Включает карусель товаров, секцию категорий и поле для поиска.
*   **Аутентификация через Firebase:** Полный цикл аутентификации пользователей:
    *   Приветственный экран-карусель (`WelcomeScreen`).
    *   Регистрация (`SignUp`) и вход (`LogIn`).
    *   Выход из системы на экране настроек.
*   **Подробная информация о товаре:** При клике на товар открывается детальный экран (`DetailScreen`) с галереей изображений, полным описанием, характеристиками (высота, размер горшка) и ценой.
*   **Навигация:** Продуманная навигация между экранами с помощью Jetpack Navigation for Compose, включая безопасную передачу данных о товаре.
*   **Отображение списков:** Экраны со списками всех товаров (`ListPlantScreen`).

## 🛠️ Архитектура и технологии

Проект следует чистой, **MVVM-подобной архитектуре**, разделяя логику на слои для лучшей читаемости, тестируемости и поддержки.

*   **Архитектура:** MVVM (View - ViewModel - Model) + Repository Pattern.
    *   **View (UI Layer):** `@Composable` функции (экраны `MainScreen`, `DetailScreen` и др.). Полностью отделены от бизнес-логики.
    *   **ViewModel:** `AuthViewModel` и `MainViewModel` управляют состоянием UI и взаимодействуют с репозиторием.
    *   **Repository:** `AuthRepository` и `MainRepository` инкапсулируют логику доступа к данным, работая с Firebase.
    *   **Model:** Data-классы (`PlantModel`, `CategoryModel`), описывающие сущности данных.

*   **Технологический стек:**
    *   **Язык:** [Kotlin](https://kotlinlang.org/)
    *   **UI:** [Jetpack Compose](https://developer.android.com/jetpack/compose) & [Material 3](https://m3.material.io/)
    *   **Асинхронность:** [Kotlin Coroutines](https://kotlinlang.org/docs/coroutines-overview.html) и [Flow](https://kotlinlang.org/docs/flow.html) (`StateFlow` для управления состоянием).
    *   **Навигация:** [Jetpack Navigation for Compose](https://developer.android.com/jetpack/compose/navigation).
    *   **Backend & DB:**
        *   [Firebase Realtime Database](https://firebase.google.com/docs/database) (для категорий и старых методов загрузки).
        *   [Firebase Authentication](https://firebase.google.com/docs/auth) (для управления пользователями).
    *   **Загрузка изображений:** [Coil](https://coil-kt.github.io/coil/compose/) (`AsyncImage`).
    *   **Жизненный цикл:** `ViewModel` из AndroidX Lifecycle.

## 🔥 Структура данных в Firebase

Для корректной работы приложения ваша база данных Firebase должна иметь следующую структуру:

### Firestore

1.  **Коллекция:** `plants`
2.  **Документы:** Каждый документ в этой коллекции представляет собой объект `PlantModel` со следующими ключевыми полями для фильтрации:
    *   `popularPlant: Boolean` (true для популярных)
    *   `newPlant: Boolean` (true для новинок)
    *   Остальные поля соответствуют data-классу `PlantModel`.

### Realtime Database

Проект также использует Realtime Database для некоторых функций. Структура должна быть такой:

```json
{
  "Category": {
    "0": { "Id": 0, "Name": "Indoor" },
    "1": { "Id": 1, "Name": "Outdoor" },
    "2": { "Id": 2, "Name": "Artificial" }
  },
  "Plants": {
    "plant_id_1": {
      "Title": "Monstera",
      "PopularPlant": true,
      "NewPlant": false,
      "CategoryId": "0"
    }
  }
}
```

## 📂 Структура проекта

*   **`navigation`**: Содержит `NavGraph.kt` (описание всех маршрутов) и `Screen.kt` (sealed class для представления экранов).
*   **`screen`**: Основная папка с UI. Каждый экран (`main`, `detail`, `auth` и др.) имеет свою подпапку с Composable-функцией экрана и папкой `components` для его составных частей.
*   **`domain`**: Data-классы (`PlantModel`, `CategoryModel`), описывающие бизнес-сущности.
*   **`viewModel`**: `MainViewModel` и `AuthViewModel`, управляющие состоянием и логикой.
*   **`repository`**: `MainRepository` и `AuthRepository`, ответственные за получение данных из Firebase.
*   **`helper`**: Вспомогательные файлы, например, `previewPlant` для превью в Android Studio.

## 📈 Возможные улучшения (Roadmap)

*   **Корзина:** Реализовать логику добавления товаров в корзину и экран оформления заказа.
*   **Избранное:** Добавить функционал для пометки товаров как "любимых" и отдельный экран для их просмотра.
*   **Поиск:** Реализовать логику поиска товаров по названию.
*   **Кеширование:** Интегрировать `Room` для кеширования данных и обеспечения работы в офлайн-режиме.
*   **Профиль пользователя:** Расширить экран настроек, добавив возможность редактирования данных профиля.
